const Calendar = tui.Calendar;

const calendar = new Calendar('#calendar', {
  defaultView: 'week',
});

const calendarSideBarOne = new Calendar('#calendarSideBarOne', {
  defaultView: 'month',
});

const calendarSideBarThree = new Calendar('#calendarSideBarThree', {
  defaultView: 'month',
});